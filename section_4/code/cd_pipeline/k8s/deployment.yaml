kind: Deployment
apiVersion: apps/v1
metadata:
  name: lior-kipod-prod
  namespace: hands-on-jenkins-app
spec:
  replicas: 1
  selector:
      matchLabels:
          app: lior-kipod
          env: dev
  template:
    metadata:
      name: lior-kipod-prod
      labels:
        app: lior-kipod
        env: dev
    spec:
      containers:
      - name: lior-kipod-prod
        image: stavco9/blabla
        #resources:
          #limits:
          #  memory: "500Mi"
        #    cpu: "100m"
        imagePullPolicy: Always
        livenessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 3
          periodSeconds: 3      
        ports:
        - name: app
          containerPort: 5000